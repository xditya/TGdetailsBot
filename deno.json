{
  "importMap": "./import_map.json",
  "tasks": {
    "test": "deno fmt -q && deno run --allow-net --allow-read --allow-env src/locals.ts",
    "serverless": "deno run --allow-net --allow-read --allow-env src/run.ts"
  }
}
